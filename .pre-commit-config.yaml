# Pre-commit hooks mirroring the CI quality gate.
#   Install once:   uv run pre-commit install
#   Run manually:   uv run pre-commit run --all-files
#   Update revs:    uv run pre-commit autoupdate
repos:
  # Ruff: lint (with autofix) + format — matches CI `ruff check` / `ruff format`.
  # Keep this rev in lockstep with the ruff pin in uv.lock. Markdown stays excluded via
  # [tool.ruff] extend-exclude = ["*.md"] (and ruff-pre-commit does not pass .md files
  # unless types_or opts in), so doc code samples are never reformatted.
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.5
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  # Standard hygiene hooks.
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: detect-private-key

  # mypy runs as a local hook via `uv run` so it uses the project environment
  # and the strict config in pyproject.toml — identical to CI (`mypy settfex/`).
  - repo: local
    hooks:
      - id: mypy
        name: mypy (settfex)
        entry: uv run mypy settfex/
        language: system
        types: [python]
        pass_filenames: false
        files: ^settfex/
